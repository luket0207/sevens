EPIC-016 Testing Log
Date: 2026-03-11

FEAT-1601 - Shared button design tokens
Manual steps:
1. Open `src/assets/scss/ui/colours.scss`.
2. Verify centralized token groups exist for primary/secondary/tertiary button variants.
3. Verify each variant includes default, hover, active, focus, and disabled-related tokens.
Expected:
- Token set exists in one shared location and is reusable by the shared button stylesheet.

FEAT-1602 / FEAT-1603 / FEAT-1604 - Primary/Secondary/Tertiary redesign
Manual steps:
1. Run app and open `/info`.
2. Navigate to UI Components -> Buttons.
3. Inspect primary/secondary/tertiary examples.
Expected:
- All three variants use refreshed teal/light-blue complementary styling.
- Primary reads as highest-emphasis CTA, secondary as medium emphasis, tertiary as lowest emphasis while still clearly button-like.
- Buttons visually separate from panel/background colours.

FEAT-1605 - Premium hover effects with stable size/position
Manual steps:
1. In `/info` Buttons section, hover primary/secondary/tertiary buttons.
2. Observe silhouette and layout footprint during hover.
Expected:
- Hover is visibly richer (sheen/glow/border/shadow/gradient transition).
- No movement, no scaling, and no layout shift occurs.

FEAT-1606 - Focus-visible and active state styling
Manual steps:
1. In `/info` Buttons section, tab-focus buttons via keyboard.
2. Press and hold/click to view active state.
3. Use "Variant State Review (debug)" row to inspect forced focus/active previews.
Expected:
- Focus-visible ring is obvious and accessible.
- Active/pressed state is visually intentional without button movement.

FEAT-1607 - Disabled state readability
Manual steps:
1. In `/info` Buttons section, inspect disabled primary/secondary/tertiary buttons.
2. Compare against enabled versions.
Expected:
- Disabled buttons remain readable but clearly inactive.
- Disabled variants still match the new design language.

FEAT-1608 / FEAT-1609 - Shared reusable system applied globally
Manual steps:
1. Open `src/engine/ui/button/button.scss`.
2. Confirm variant styling is tokenized via shared mixin/system (not page-level one-offs).
3. Navigate through app screens that use `Button` and verify the refreshed styles appear.
Expected:
- Shared button system drives variant visuals across existing button usage.
- Existing button behaviors/flows remain intact.

FEAT-1610 - Visual review/debug support
Manual steps:
1. Open `/info` -> UI Components -> Buttons.
2. Locate "Variant State Review (debug)" matrix.
3. Verify rows for primary/secondary/tertiary and columns for default/hover/active/focus/disabled.
Expected:
- All key button states are inspectable in one place during implementation/review.

Command checks run:
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope (prop-types and hook-rule errors in pre-existing files).
- `npm run build`: passed.

Automated tests completed:
- None added for this styling-focused epic.

Automated tests still needed (optional follow-up):
- Visual regression snapshots for button variants/states to protect design system changes over time.

Test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

FEAT-1611 - Bespoke context-aware Continue button for Career Calendar
Manual steps:
1. Open `/career/home`.
2. Inspect the Continue action panel in the right-side Career Controls section.
3. Verify the continue action is no longer rendered as the shared generic `Button` style and instead appears as a bespoke 100px action-card style control.
4. Verify label text still reflects continue-flow context (examples: Match, Cup Draw, Fixture Generation, Next Day).
5. Verify icon behavior:
   - draw actions show draw icon (box-open)
   - cup/match contexts show event icon from calendar visual system
   - non-event actions (for example Next Day / Team Setup) show fallback arrow icon.
6. Verify event-based colour treatment updates according to action context (league/cup/draw/fallback style families).
7. Hover/focus/press the bespoke continue button and verify:
   - premium visual response is present
   - no scale, no movement, and no layout shift occurs
   - control remains fixed at 100px height.
8. Trigger continue flow from each major branch (match day, cup draw day, normal next-day flow) and confirm routing/behavior remains unchanged.
Expected:
- Continue action next to the calendar uses bespoke component and not shared button.
- Context-aware label logic remains correct.
- Icon and styling are visibly tied to the upcoming event type.
- Fallback arrow icon appears when no specific event icon applies.
- Hover/active/focus behavior matches premium language without changing size/position.

Command checks run for FEAT-1611:
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors introduced by FEAT-1611 changes.
- `npm run build`: passed.

FEAT-1611 test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 1)

BUG-001 - Continue button subtle animated ambient glow
Manual steps:
1. Open `/career/home`.
2. Observe continue button in idle state for a few seconds.
3. Move through contexts that change continue-button colour theme (league/cup/draw/fallback).
4. Hover/focus/press the continue button, then return to idle state.
Expected:
- Idle button shows subtle continuous glow animation.
- Glow colour follows active button context colour.
- Hover/focus/active states remain clear and are not overwhelmed by ambient animation.
- Disabled state has no ambient animation.

Command checks run in update-1 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by this glow animation update.

Update-1 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 2)

BUG-002 - Increase visible animated edge glow
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle state and focus on outer button edges.
3. Compare glow readability across continue-button context colours (league/cup/draw/fallback).
4. Hover/focus/active the button and verify interaction states still read clearly.
Expected:
- Edge glow is visibly readable in idle state.
- Glow remains colour-matched to current button context.
- Interaction states still override/clarify presentation without excessive glow.

Command checks run in update-2 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-2 glow visibility adjustments.

Update-2 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 3)

BUG-003 - Stronger glow that remains on hover
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle state and confirm edge glow readability.
3. Hover the continue button and keep pointer steady.
4. Check multiple continue contexts (league/cup/draw/fallback) where available.
Expected:
- Ambient glow is stronger and clearly readable in idle state.
- Ambient glow remains visible during hover.
- Glow remains colour-matched to current context and interaction states still read clearly.

Command checks run in update-3 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-3 glow/hover refinements.

Update-3 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 4)

BUG-004 - Stronger glow with more breathing room and 125px height
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle animation and compare glow visibility to previous iteration.
3. Confirm the button has additional margin around it inside the controls panel.
4. Confirm button height now renders at 125px and width is slightly reduced from full panel width.
5. Hover the button and verify glow remains visible and readable.
Expected:
- Glow animation is more noticeable in idle state.
- Hover state retains strong visible glow.
- Button has more surrounding space so glow is less cramped against panel edges.
- Continue button height is 125px.

Command checks run in update-4 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-4 glow/sizing adjustments.

Update-4 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 5)

BUG-005 - Concentrated glow + increased container gap
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle glow and compare spread around edges.
3. Hover the continue button and inspect glow spread.
4. Verify spacing between button boundary and controls-panel container edges.
Expected:
- Glow is visually tighter/more concentrated, with less wide diffuse spill.
- Hover glow remains visible but not overly spread out.
- Button has more surrounding gap from the container.

Command checks run in update-5 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-5 glow concentration/spacing refinements.

Update-5 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 6)

BUG-006 - Glow closer to hard-line with slight fade
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle glow edge profile.
3. Hover the continue button and compare glow spread to idle.
Expected:
- Glow reads as a tighter edge line with only slight fade.
- Hover glow remains visible without broad diffuse spread.

BUG-007 - Remove "Next Action" text
Manual steps:
1. Open `/career/home`.
2. Inspect continue button text content.
Expected:
- "Next Action" text is not displayed.
- Action label remains visible and unchanged.

Command checks run in update-6 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-6 glow/text refinements.

Update-6 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 7)

BUG-008 - Glow pulse travels further out from edge
Manual steps:
1. Open `/career/home`.
2. Observe continue button pulse from base state to peak.
3. Compare peak glow reach against the tighter base glow ring.
Expected:
- Peak phase visibly travels further outward from edge than base state.
- Base state remains concentrated while animation shows clear outward movement.

Command checks run in update-7 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-7 pulse-distance refinement.

Update-7 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 8)

BUG-009 - Sonar-style outward pulse
Manual steps:
1. Open `/career/home`.
2. Observe continue button idle glow animation for multiple cycles.
3. Verify animation reads as an outward travelling ring pulse from the button edge.
4. Hover the button and ensure pulse remains readable.
5. Focus/active/disabled the button and ensure interaction states remain clear.
Expected:
- Animation reads like a sonar pulse wave expanding from button edge.
- Pulse remains colour-matched to current button context.
- Interaction states stay readable and are not visually overwhelmed.

Command checks run in update-8 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-8 sonar-pulse refinement.

Update-8 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)

Bug fix regression pass (2026-03-11, update 9)

BUG-010 - Restore hover clipping behavior
Manual steps:
1. Open `/career/home`.
2. Hover the continue button and observe sheen/highlight behavior.
3. Compare button edge rendering during hover against idle state.
Expected:
- Hover sheen is clipped cleanly to the button shape.
- Hover visual treatment no longer appears visually broken at edges.

BUG-011 - Cap sonar pulse travel at 12px
Manual steps:
1. Open `/career/home`.
2. Observe continue-button sonar pulse over several cycles.
3. Check the furthest outward ring distance from button edge.
Expected:
- Sonar ring fades out by ~12px max from the button edge.
- Pulse remains visible/readable but no longer overextends.

Command checks run in update-9 bug-fix pass:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository-wide lint baseline unrelated to EPIC-016 scope.
  - No new EPIC-016-specific lint errors were introduced by update-9 hover/sonar refinements.

Update-9 bug-fix test status: in progress (manual steps documented, not executed in this shell session), passed (build), failed (lint baseline unchanged)
