EPIC-008 Testing Log
Date: 2026-03-08

Command checks run:
- `npm run build`: passed.
- `npm run lint`: failed on existing repository-wide baseline issues in unrelated shared engine/info modules.

Manual verification note:
- Full browser/manual verification is still pending. The checks below define expected behaviour for each implemented feature.

FEAT-801 - Create Team Management Page Structure
Manual steps:
1. Open `/team-management` from Career Home control panel.
Expected:
- Page has tactic box at top, player list left, pitch right, save/cancel area, and debug panel.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed on unrelated baseline issues.
Automated needed:
- Component/layout test for Team Management structural regions.
Status: in progress

FEAT-802 - Build Pitch Layout with Fixed Goalkeeper and 6 Role Slots
Manual steps:
1. Open Team Management page.
Expected:
- Goalkeeper is fixed in goal and not draggable.
- Six outfield role slots exist (2 DF, 2 MD, 2 AT).
Automated completed:
- `npm run build`: passed.
Automated needed:
- Component test for fixed GK + six role slot rendering.
Status: in progress

FEAT-803 - Build Player List for Available Outfield Players
Manual steps:
1. Inspect left player list panel.
Expected:
- Outfield players are clearly visible with identity/rating info.
- Players can be picked up (drag start) from list.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Component test for player list rendering and drag-start hooks.
Status: in progress

FEAT-804 - Implement Player Drag and Drop / Placement Interaction
Manual steps:
1. Drag player from list to empty pitch slot.
2. Drag player from one pitch slot to another.
3. Drag a pitch player and drop anywhere that is not a valid slot.
Expected:
- List -> slot placement works.
- Slot -> slot movement works.
- Off-slot drop removal works and player returns to list.
- No player duplication occurs.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Integration tests for list->slot, slot->slot, and slot->unplace flows.
Status: in progress

FEAT-805 - Track Team Arrangement State and Team Completeness
Manual steps:
1. Fill and unfill outfield slots via drag/drop.
Expected:
- Slot state updates correctly.
- Completeness is true only when all 6 slots are filled.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Unit tests for completeness and unplaced-player derivation.
Status: in progress

FEAT-806 - Build Tactic Box UI
Manual steps:
1. Inspect tactic box controls.
2. Change each defensive/attacking width and tactic control.
Expected:
- All required controls and options are present and selectable.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Component test for tactic select option sets.
Status: in progress

FEAT-807 - Store Tactical Settings in Team Management State
Manual steps:
1. Change tactic and width values.
2. Trigger rerenders by moving players.
Expected:
- Tactic selections persist in page state and remain current.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Unit test for tactic state update and persistence in draft state.
Status: in progress

FEAT-808 - Define Tactic Calculation Input Model
Manual steps:
1. Arrange players across DF/MD/AT slots.
2. Inspect grouped totals in debug panel.
Expected:
- DF/MD/AT/ALL grouped skill totals reflect slot role grouping correctly.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Unit tests for grouped-total calculations by skill and role.
Status: in progress

FEAT-809 - Implement Defence Tactic Rating (DTR) Calculation
Manual steps:
1. Change defensive tactic with stable player arrangement.
2. Inspect DTR value and debug grouped totals.
Expected:
- DTR changes according to defensive matrix formulas.
- DTR is divided by 10, rounded, and clamped to 1..100.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Unit tests per defensive tactic formula path.
Status: in progress

FEAT-810 - Implement Attack Tactic Rating (ATR) Calculation
Manual steps:
1. Change attacking tactic with stable player arrangement.
2. Inspect ATR value and debug grouped totals.
Expected:
- ATR changes according to attack matrix formulas.
- ATR is divided by 10, rounded, and clamped to 1..100.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Unit tests per attacking tactic formula path.
Status: in progress

FEAT-811 - Display DTR and ATR Debug Output
Manual steps:
1. Change tactics and slot assignments.
Expected:
- DTR and ATR are always visible and update immediately on state changes.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Component test asserting live updates for DTR/ATR display.
Status: in progress

FEAT-812 - Add Save and Cancel Actions with Confirmation
Manual steps:
1. Click Save when complete.
2. Click Cancel at any time.
Expected:
- Both actions show confirmation modal.
- Save only enabled when all 6 outfield slots are filled.
- Confirmed Save returns to calendar.
- Confirmed Cancel returns to calendar without persisting draft.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Integration tests for save/cancel modal-confirmation flow.
Status: in progress

FEAT-813 - Persist Team Management Changes to Career State
Manual steps:
1. Save a modified arrangement/tactic setup.
2. Re-open Team Management page.
Expected:
- Saved slot assignments, tactics, DTR, and ATR are persisted in `career.world.playerTeam.teamManagement`.
- Re-opened page reflects saved setup.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Integration test for save persistence + reload of saved lineup/tactics.
Status: in progress

FEAT-814 - Connect Team Management Page to Career Navigation
Manual steps:
1. Open Team Management via Career Home.
2. Exit via Save and Cancel.
Expected:
- Page is reachable from career flow.
- Save/Cancel both return to `/career/home`.
- Guard redirects work when generation state is invalid.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Route integration tests for reachability and guard redirects.
Status: in progress

FEAT-815 - Add Team Management Debug/Test Utilities
Manual steps:
1. Inspect Team Management debug panel while changing tactics/slots.
Expected:
- Debug view includes slot assignments, unplaced players, grouped totals, tactics, DTR, ATR, completeness.
Automated completed:
- `npm run build`: passed.
Automated needed:
- Component snapshot test for debug panel state output.
Status: in progress

----------------------------------------------------------------
BUG-001 Regression Test - Pitch Cards Simplified and Position-Coloured Pills
Manual steps:
1. Open `/team-management`.
2. Place players into DF, MD, and AT slots.
3. Inspect fixed goalkeeper card and outfield cards on pitch.
4. Inspect at least one empty slot.
Expected:
- Filled pitch slots show image + one name/overall pill only.
- GK pill is orange, DF yellow, MD blue, AT red.
- Empty slots show shirt-shadow placeholders with position label inside.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Visual/component regression test for pitch tile variants and role-colour class mapping.
Status: passed

----------------------------------------------------------------
BUG-002 Regression Test - Player List Always Shows All Outfield Players
Manual steps:
1. Open `/team-management` with no saved team-management state.
2. Confirm all outfield players are shown in list initially.
3. Place several players on pitch.
4. Re-check list visibility and placement indicators.
Expected:
- All outfield players remain visible in list at all times.
- Placed players are visually altered and show placement label.
- Team can still be completed/saved through drag/drop workflow.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Unit tests for initial empty slot draft behavior and list-placement mapping.
- Integration test ensuring placed players remain rendered in list with visual state.
Status: passed

----------------------------------------------------------------
BUG-003 Regression Test - Drag Preview Should Be Player Image Only
Manual steps:
1. Open `/team-management`.
2. Start dragging a player from list.
3. Start dragging a player from pitch.
Expected:
- Drag preview in both cases is only the player image.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Browser-level drag-and-drop test for custom drag image rendering.
Status: passed

----------------------------------------------------------------
BUG-004 Regression Test - Full Skillset Visible in List Entries
Manual steps:
1. Open `/team-management`.
2. Inspect each outfield player row in list.
Expected:
- List entry shows full available skillset values, not only a partial subset.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Component test asserting full-skill text rendering from `player.skills` object.
Status: passed

----------------------------------------------------------------
BUG-005 Regression Test - Off-Slot Drop Removes Placed Player
Manual steps:
1. Place a player in a pitch slot.
2. Drag that player and drop anywhere that is not a valid slot.
3. Confirm player is unplaced and pitch slot clears.
4. Confirm dedicated remove drop-zone is no longer present.
Expected:
- Player is removed from slot on non-slot drop.
- No explicit "Drop a pitch player here to remove from pitch" area exists.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Integration test for slot drag-end non-slot drop removal behavior.
Status: passed

----------------------------------------------------------------
BUG-006 Regression Test - Slot Position Label Remains Visible When Filled
Manual steps:
1. Place players into DF/MD/AT slots.
2. Inspect the top center area of each filled slot.
Expected:
- Slot position label remains visible in top center for both empty and filled slots.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Visual/component test for persistent slot-tag visibility across slot states.
Status: passed

----------------------------------------------------------------
BUG-007 Regression Test - Skillset Uses Engine Bars
Manual steps:
1. Open `/team-management`.
2. Inspect one or more player entries in the list.
Expected:
- Each skill row uses engine bar UI (track + fill + value).
- Full numeric skillset is visible in bar form for list entries.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Component test for `PlayerSkillsetBars` rendering all numeric skills with bar rows.
Status: passed

----------------------------------------------------------------
BUG-008 Regression Test - Goalkeeper Visible in Player List
Manual steps:
1. Open `/team-management`.
2. Inspect left player list for goalkeeper row.
3. Attempt to drag goalkeeper from list.
Expected:
- Goalkeeper appears in list with stats visible.
- Goalkeeper list entry is non-draggable and marked as fixed/reference.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Integration test that list player source includes goalkeeper while slot drag eligibility stays outfield-only.
Status: passed

----------------------------------------------------------------
BUG-009 Regression Test - Skill Bars Continuous and Value-Coloured
Manual steps:
1. Open `/team-management`.
2. Inspect skill bars for low, mid, and high values.
Expected:
- Bars are continuous (no segment dividers).
- Fill colour changes by value band, using rating scale colours.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Component/visual regression test asserting no segmented class and correct band class-to-colour mapping.
Status: passed

----------------------------------------------------------------
BUG-010 Regression Test - 50/50 Layout, No Pitch Header, Scrollable List
Manual steps:
1. Open `/team-management` on desktop width.
2. Compare width of player list panel and pitch panel.
3. Inspect pitch panel top area for heading text.
4. Verify pitch panel starts at top alignment.
5. Verify player list content scrolls internally when it exceeds max height.
Expected:
- Player list and pitch panels each occupy roughly half of the main row width.
- Pitch header/subheading text is not rendered.
- Pitch/list columns align to top.
- Player list has controlled max height with vertical scrollbar when needed.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Visual regression test for 50/50 layout proportions and absence of pitch header text.
- Component/style test for list overflow behavior (`max-height` + `overflow-y`).
Status: passed

----------------------------------------------------------------
BUG-011 Regression Test - Pitch and List Maintain Matching Height
Manual steps:
1. Open `/team-management` on desktop width.
2. Compare full panel heights of player list and pitch.
3. Scroll player list with enough content and re-check panel heights.
Expected:
- Player list and pitch panels remain the same height.
- Player list content scrolls internally without changing overall panel height.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Visual regression test asserting equal column heights at desktop breakpoint.
- Component/style test for column height sync plus internal list overflow behavior.
Status: passed

----------------------------------------------------------------
FEAT-816 - Position Markers and Ordered Placed Players in List
Manual steps:
1. Open `/team-management`.
2. Place players into Left DF, Right DF, Left MD, Right MD, Left AT, Right AT slots.
3. Inspect player list ordering and each placed entry label.
Expected:
- Player list entries for placed players show position marker labels.
- Positioned players are sorted at top in this order:
  - GK
  - Left DF
  - Right DF
  - Left MD
  - Right MD
  - Left AT
  - Right AT
- Unplaced players remain below positioned players.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Integration test asserting ordered list output after slot assignments are changed.
- Component test asserting placed marker text mapping for GK and left/right outfield slots.
Status: passed

----------------------------------------------------------------
BUG-012 Regression Test - Position Marker Badge is Top-Right and Obvious
Manual steps:
1. Open `/team-management`.
2. Place at least one outfield player and inspect that list entry.
3. Inspect goalkeeper entry in list.
Expected:
- Position marker is clearly visible as a badge.
- Badge is anchored at the top-right of each placed/fixed list card.
- Marker text shows the position label (for example `GK`, `Left DF`, etc.).
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Visual regression test asserting top-right placement of marker badge.
- Component test asserting marker badge text rendering for placed players.
Status: passed

----------------------------------------------------------------
FEAT-817 - Defensive vs Attacking Tactic Compatibility Value
Manual steps:
1. Open `/team-management`.
2. Set defensive tactic and attacking tactic to a known matrix pair.
3. Verify displayed compatibility value against matrix.
4. Change either tactic and verify compatibility updates immediately.
Expected:
- A third tactic output appears alongside DTR and ATR.
- Value is taken only from tactic pair matrix (not player skill changes).
- Example checks:
  - Low Block + Counter => 80
  - Offside Trap + Direct => 80
  - Mid Block + Direct => 10
- Debug output includes `tacticCompatibility`.
Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues in engine/info files.
Automated needed:
- Unit tests for `calculateTacticCompatibility` matrix lookup coverage.
- Integration test for live compatibility value updates when tactic selects change.
Status: passed
