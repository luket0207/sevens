EPIC-010 Testing Log
Date: 2026-03-08

Command checks run:
- `npm run build`: passed.
- `npm run lint`: failed on existing repository-wide baseline issues in unrelated engine/info modules.

FEAT-1001, FEAT-1002, FEAT-1008 - League Table UI + Standings Data/Updates
Manual steps:
1. Start a new career and wait for generation completion.
2. Open Career Home.
3. Verify League Table panel is visible and defaults to player's league.
4. Switch leagues from dropdown and inspect columns: position, team, played, won, drawn, lost, GF, GA, GD, points.
5. Advance through at least one league match day.
Expected:
- Player row is marked with icon.
- Table sorting and values update after simulated match days.

FEAT-1003, FEAT-1004, FEAT-1005, FEAT-1006, FEAT-1007 - Match Rating + MRD Matrix + Scoreline + AI Day Simulation + Loading State
Manual steps:
1. On Career Home, click Continue Day across multiple match and non-match days.
2. During simulation days, confirm loading feedback appears.
3. Open debug panel and inspect simulation logs for:
   - home/away match ratings
   - MRD and band
   - selected result type
   - generated scoreline.
Expected:
- AI fixtures resolve automatically when no player fixture intervention is needed.
- Debug output shows matrix-driven resolution values.

FEAT-1009, FEAT-1010, FEAT-1011, FEAT-1012, FEAT-1013 - Cup Structures, Simulation, and Cups Page
Manual steps:
1. Open `/career/cups` from Career Home controls.
2. Verify League Cup stages and fixtures/results are visible.
3. Verify Champions Cup groups, staged fixtures, and results are visible.
4. Continue days through cup matchdays and re-check Cups page.
Expected:
- Cup fixtures resolve and progression stages populate over the season.
- Cup state remains inspectable even without player cup participation.

FEAT-1014, FEAT-1015 - Player Match-Day Routing and Temporary Match Resolution
Manual steps:
1. Advance to a day with player fixture.
2. Click Continue Day.
3. Verify app routes to `/match` instead of skipping day.
4. Select each resolution option (Home Win / Draw / Away Win in separate runs).
5. Return to Career Home and inspect standings/debug.
Expected:
- Player fixture is not auto-simulated before user selection.
- Selected outcome produces valid scoreline and persists into world state.
- Career flow continues after resolution.

FEAT-1016, FEAT-1017, FEAT-1018 - Season Outcomes Persistence and Debug/Inspection
Manual steps:
1. Continue day progression to season completion.
2. Inspect `career.calendar.simulation.seasonOutcomes` in debug JSON.
3. Confirm promotion/relegation/playoff results and League 1 top-4 qualification are present.
4. Reload/navigate away and back, then re-check simulation state.
Expected:
- End-of-season outcomes are stored in career state.
- League/cup fixtures, results, and debug traces remain persisted.

Automated completed:
- `npm run build`: passed.
- `npm run lint`: failed due to existing unrelated baseline lint issues.

Automated still needed:
- Unit tests for MRD band selection and weighted outcome resolution.
- Unit tests for standings application and tie-break sorting.
- Integration tests for day simulation pipeline (AI fixtures + player routing).
- Integration tests for League Cup/Champions Cup stage progression and persistence.
- Integration tests for season-end promotion/relegation/playoff outcome generation.

Test status: passed

BUG-028 - Player-team match rating alignment with saved Team Management setup
Manual steps:
1. Open Team Management, fill all six outfield slots, set tactics, and save.
2. Simulate to a match day and open day-results/debug output containing match-profile data.
3. Compare player-team MR inputs (`ov`, `dtr`, `atr`, `tc`) against AI teams in the same league.
4. Confirm player-team `dtr`/`atr` are no longer near-minimum due to missing slot assignments.
Expected:
- Player-team profile uses saved `teamManagement.slotAssignments` for match-profile tactic rating calculations.
- Player-team MR values are in a comparable range to similarly rated league teams instead of being artificially depressed.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Unit test for `buildTeamMatchProfile` verifying player-team assignment source prioritizes saved team-management slots over `squadSlot`.
- Integration test comparing match-profile output before/after saved team-management setup.

Test status: passed

BUG-029 - Cup-draw continue blocked until reveal completion
Manual steps:
1. Open `/cup-draw` on any draw day (day 1 and a later cup-draw day).
2. Reveal only part of fixtures on at least one draw card.
3. Verify top and bottom `Continue` buttons remain disabled.
4. Reveal all fixtures for all draw cards.
5. Verify top and bottom `Continue` buttons become enabled and progression works.
Expected:
- Continue remains blocked until all draw fixtures are revealed.
- Behavior is consistent across day-1 and non-day-1 draw flows.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Integration test for `/cup-draw` gating that asserts continue disabled until `allDrawsRevealed === true`.

Test status: passed

BUG-027 - Updated Match Result Type Matrix bands and values
Manual steps:
1. Open `src/game/careerSimulation/constants/simulationConstants.js`.
2. Verify `MRD_RESULT_MATRIX` contains these band IDs in order:
   - `0-5`, `5-12`, `12-25`, `25-35`, `35-50`, `50-100`, `100-150`, `150-300`, `300+`.
3. For each row, verify probabilities match the requested values.
4. Simulate several career days and inspect match-result spread in day results/debug to confirm simulation runs with updated matrix and no runtime errors.
Expected:
- New MRD bands and values are present exactly as requested.
- Match simulation continues to resolve outcomes without errors using the new matrix.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Unit tests for `resolveMrdBand` coverage across each new boundary (`5`, `12`, `25`, `35`, `50`, `100`, `150`, `300`).
- Simulation distribution test to sanity-check weighted outcomes for each band over large sample runs.

Test status: passed

BUG-022 - Team overall shown in squad modal header
Manual steps:
1. Open Career Home and click a team in the League Table.
2. Confirm squad modal opens.
3. Check modal title text.
Expected:
- Modal title includes team overall value in header (for example `... | OVR 73`).

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-021 - Day-1 draw controls (Generate Cup / Generate Leagues / Generate All) and gated continue
Manual steps:
1. Start a fresh career and advance to day 1 draw flow (`/cup-draw`).
2. Verify a day-1 control panel is shown with three buttons:
   - `Generate Cup`
   - `Generate Leagues`
   - `Generate All`.
3. Confirm per-draw `Reveal Next Fixture` / `Reveal All` buttons are not shown in day-1 mode.
4. Confirm `Continue` is disabled before reveals are completed.
5. Click `Generate Cup` and verify cup draw cards become revealed while league fixture cards remain unrevealed.
6. Click `Generate Leagues` and verify league fixture cards reveal.
7. Verify `Continue` becomes enabled only when all draw cards are fully revealed.
8. Repeat with `Generate All` and confirm one-click full reveal with enabled continue.
Expected:
- Day 1 uses grouped generation actions instead of league-by-league reveal controls.
- Continue progression is blocked until draw results are shown.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-020 - Show goalscorers and assisters in match-day results
Manual steps:
1. Simulate to a day with one or more non-zero scorelines, then open `/career/day-results`.
2. Inspect each match result entry with goals.
3. Verify goal lines appear beneath the scoreline in minute order.
4. Confirm each line includes scorer name and shows assister text when available.
5. Verify entries with no assists do not display assist text.
Expected:
- Goal scorers are visible for each goal in the match-day results view.
- Assisters are shown for goals that have an assister.
- Existing scoreline and MR/MRD lines remain intact.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-019 - Champions Cup pre-match draw visibility, day-1 group draw, and no day-1 results panel
Manual steps:
1. Start a fresh career and advance from day 1.
2. Confirm flow routes to `/cup-draw` on day 1 and includes:
   - Champions Cup Group Draw card
   - season fixture draw cards for domestic leagues.
3. Confirm day 1 does not route to `/career/day-results`.
4. Continue season progression to Champions Cup knockout draw windows.
5. Confirm Champions Cup knockout draws appear on draw days before the match day and are labelled as draws.
Expected:
- Day 1 uses draw flow (not results panel) for group draw + season fixture reveal.
- Champions Cup draws are visible before fixture days and presented like cup draws.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

FEAT-1023 - Goal events, assists, clean sheets, and competition-scoped player stat tables
Manual steps:
1. Start/continue a career and simulate through multiple league and cup fixture days.
2. Open Career Home debug panel and inspect `simulation.debug.playerStatsSnapshot`.
3. Confirm league table entries exist under `leagueTablesByCompetition` and cup entries under `cupTablesByCompetition`.
4. Inspect resolved fixture payloads in simulation debug logs and confirm `goalEvents` exist for scored matches.
5. Verify each goal event includes:
   - scorer
   - minute (1-90)
   - assister or null.
6. Spot-check minutes in a single match to confirm no duplicate goal minute values.
7. Verify clean-sheet counts increase for goalkeepers in fixtures where their team concedes 0.
Expected:
- Goal events are generated and stored on simulated fixture results.
- Scorers/assists/clean sheets update in persistent simulation state.
- League and cup stat tables remain separated by competition scope.

FEAT-1024 - Team squad modal from league table
Manual steps:
1. Open Career Home and locate the League Table panel.
2. Click multiple team names (including player team and AI teams).
3. Confirm modal opens with team-specific squad details.
4. Verify each player card shows:
   - player name
   - role context
   - overall
   - full skill bars.
5. Confirm the player list area scrolls vertically when content exceeds modal height.
6. Close modal using close icon and verify return to league table context.
Expected:
- League-table teams are clickable and open a readable squad modal.
- Existing player image/skill display components are reused.
- Modal remains usable with scroll for full-squad inspection.

Command checks run after this update:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository-wide baseline lint issues in engine/info modules.

Automated still needed:
- Unit tests for weighted scorer/assister role selection and unique-minute generation.
- Unit tests for player stat accumulation and competition scoping.
- Integration tests for league-table team click to modal flow.

Test status: passed

FEAT-1022 - Team form and home-advantage modifiers in match simulation
Manual steps:
1. Start a new career and open simulation debug after several days.
2. Confirm simulation state contains `teamFormByTeamId` with arrays of `W/D/L` values.
3. Confirm foreign teams start with non-empty 5-result form arrays.
4. Progress through at least 5 played matchdays and confirm domestic teams now carry rolling 5-result form arrays.
5. Inspect a resolved fixture debug entry and verify:
   - home/away form win counts are present
   - form win difference and mapped bonus are present
   - home-advantage bonus is present
   - adjusted probability distribution is valid.
6. Continue to season end, click `Finish Season`, then `Start New Season`.
7. Verify form state persists into the new season simulation state (not reset to empty).
Expected:
- Team form exists in persistent career simulation state for all teams.
- Form updates after every completed fixture and stays rolling to last 5.
- Home side receives +2% win bias and stronger-form side receives mapped bonus.
- Form carries into next season.

Command checks run after this feature update:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Unit tests for form utility functions (`normalise`, `append`, `bonus mapping`, `scoreline mapping`).
- Unit tests for probability adjustment balancing (form + home modifiers with matrix rows).
- Integration test covering cross-season form persistence through season summary transition.

Test status: passed

BUG-018 - Cups page includes Back to Calendar button
Manual steps:
1. Navigate to `/career/cups`.
2. Confirm a `Back to Calendar` button is visible at the top of the page.
3. Click `Back to Calendar`.
Expected:
- Navigation returns directly to `/career/home`.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-016 - Champions Cup knockout draws occur on Thursday before fixtures
Manual steps:
1. Simulate through Champions Cup group stages to qualify teams for knockout rounds.
2. Advance day-by-day around quarter-final, semi-final, and final draw windows.
3. Confirm cup-draw screen appears on Thursday before each knockout matchday.
4. Continue to knockout fixture day and confirm no same-day draw trigger appears.
Expected:
- Draw days trigger on Thursday before knockout fixtures.
- Knockout fixtures are still created and played on their scheduled match days.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-017 - Season completion flow and next-season transition
Manual steps:
1. Simulate through season end and return to Career Home.
2. Verify primary action reads `Finish Season` and is enabled.
3. Click `Finish Season` and confirm `/career/season-summary` opens.
4. Verify summary sections render:
   - league winners
   - cup winners
   - promotions/relegations.
5. Click `Start New Season`.
6. Verify return to `/career/home` with a rebuilt season calendar and fresh simulation state.
7. If player team was promoted/relegated, verify player team league/table context matches the moved league.
Expected:
- Season-end no longer blocks progression.
- Summary page is shown before next season.
- New season starts with promotion/relegation changes applied to AI and player teams.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-005 - Match-day fixture rows rendered as paragraphs
Manual steps:
1. Start a new career and advance to day 1 fixture reveal.
2. Expand a league accordion panel.
3. Inspect fixture rows under each `Match Day` section.
Expected:
- Rows appear as plain text lines (`Team A v Team B`) rendered from paragraph elements.
- No bullet list styling appears for match-day fixture rows.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-006 - Accordion panel can be toggled closed
Manual steps:
1. Start a new career and advance to day 1 fixture reveal.
2. Expand any league accordion panel.
3. Click the same league header again.
4. Confirm no league panel content is visible.
5. Click a different league header and confirm that panel opens.
Expected:
- Open panel toggles closed when clicked again.
- Accordion supports a no-open-panels state.
- Clicking another league opens that league panel normally.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-007 - Player team highlighted in fixture reveal
Manual steps:
1. Start a new career and continue to day 1 fixture reveal.
2. Expand the player's league accordion panel.
3. Locate fixtures involving the player's team.
4. Expand a non-player league panel and compare team-name styling.
Expected:
- Player team name is highlighted in fixture rows.
- Non-player teams keep the default fixture-row styling.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-008 - Matching Continue buttons top and bottom
Manual steps:
1. Advance to `/career/day-results` via normal career flow.
2. Verify a `Continue` button is visible at the top of the page.
3. Verify a second matching `Continue` button is visible at the bottom.
4. Click the top button and confirm day progression behaves as expected.
5. Re-run and click the bottom button to confirm identical behavior.
Expected:
- Top and bottom `Continue` buttons are both present.
- Both buttons execute the same day-advance/navigation behavior.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-009 - Match-day result scoreline inline format
Manual steps:
1. Advance to `/career/day-results` with at least one completed fixture.
2. Inspect a result row.
Expected:
- Each row is formatted inline as `Team A 2 - 1 Team B`.
- Score appears between home and away team names on one centered line.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-010 - Group results by competition using centered paragraph lines
Manual steps:
1. Advance to `/career/day-results` on a day with results from multiple competitions.
2. Verify result lines are grouped under competition headings.
3. Confirm each match is shown as a plain paragraph line (no bullet list / no per-match pod card).
4. Confirm result text is centered.
Expected:
- Competition grouping is present.
- Matches render as centered paragraph list lines.
- Per-match pod styling is removed.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-011 - Score column alignment in grouped results
Manual steps:
1. Advance to `/career/day-results` with several completed fixtures under one competition.
2. Inspect the score values across rows.
3. Confirm home and away team names sit on either side of the score values.
Expected:
- Score values align in a consistent vertical center column.
- Team names are positioned beside the aligned score column (home on left side, away on right side).

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-012 - Player team highlighted in match-day result rows
Manual steps:
1. Advance to `/career/day-results` on a day including the player's fixture.
2. Open the relevant competition group in the results section.
3. Inspect the home/away team names in the player's result row.
Expected:
- Player team name is highlighted in the result line.
- Non-player team names keep the default result-line style.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-013 - Updated MRD chance table to reduce draws
Manual steps:
1. Simulate multiple career days with AI fixtures.
2. Review day-result distributions and compare draw frequency to previous behavior.
3. Spot-check several MRD bands in simulation logs/debug.
Expected:
- Draw outcomes are less frequent in lower-MRD bands according to updated matrix values.
- Other outcome categories still resolve without errors.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-014 - Display match ratings and positive MRD for each played match
Manual steps:
1. Advance to `/career/day-results` on any day with completed fixtures.
2. Inspect each result row.
3. Confirm metadata line displays `MR: <home> v <away> | MRD: <value>`.
4. Confirm MRD is always non-negative.
Expected:
- Every result row includes both team match ratings and MRD.
- MRD is shown as a positive value.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-015 - Updated MRD band split and probability table
Manual steps:
1. Simulate multiple career days across mixed team-strength fixtures.
2. Inspect debug/result distributions to confirm outcomes follow the updated MRD bands:
   - `0-10`, `10-30`, `30-60`, `60-100`, `100-150`, `150-300`, `300+`.
3. Verify no runtime issues occur when MRD falls in newly split ranges (`30-60`, `60-100`).
Expected:
- Outcome resolution works for all revised bands.
- Draw frequency reflects the updated row probabilities.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-004 - Match-day grouped fixture reveal format
Manual steps:
1. Start a new career and advance to day 1 fixture reveal.
2. Expand a league accordion panel.
3. Verify fixtures are grouped under `Match Day N` headings.
4. Verify each fixture row is shown as `Team A v Team B`.
5. Verify match day groups are shown in ascending order.
Expected:
- Fixture reveal is grouped by match day.
- Fixture line format matches requested `Team A v Team B`.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-003 - Season fixture reveal accordion behavior
Manual steps:
1. Start a new career and advance through day 1 to the season fixture reveal screen.
2. Verify fixture groups are presented as league accordion panels.
3. Confirm player's current league panel is expanded by default.
4. Click a different league panel header.
5. Confirm only the clicked panel is expanded and the previous panel collapses.
Expected:
- League groups are sorted and rendered as an accordion.
- Exactly one panel is expanded at any moment.
- Player league is the default expanded panel.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

FEAT-1019 - End-of-Day Results Screen
Manual steps:
1. Advance to a day where only AI fixtures are played.
2. Click Continue Day.
3. Verify `/career/day-results` opens and lists all completed matches for the day.
4. Click Continue and confirm return to Career Home on the next day.
Expected:
- Results screen appears for match days.
- Continue exits results view and advances calendar flow.

FEAT-1020 - Day-1 Season Fixture Generation/Display
Manual steps:
1. Start a fresh season and click Continue Day on day 1.
2. Verify end-of-day screen includes full season fixture list grouped by league.
3. Click Continue and verify normal calendar progression resumes.
Expected:
- Day-1 flow displays season fixture schedule sorted by league.
- Fixture schedule remains persisted and is used for later match simulation.

FEAT-1021 - Cup Draw Resolution Flow
Manual steps:
1. Advance to a calendar day with `Cup Draw` event.
2. Verify app routes to `/cup-draw`.
3. Use `Reveal Next Fixture` repeatedly for one draw, then `Reveal All` on another draw.
4. Click Continue and verify flow returns to pending day-results (if any) or advances to next calendar day.
Expected:
- Draw pairings are displayed clearly.
- User can reveal fixtures incrementally or all at once.
- Generated pairings remain stored and are used for future cup rounds.

BUG-001 / BUG-002 Regression Verification
Manual steps:
1. Generate a new career.
2. Confirm League Table appears immediately on Career Home.
3. Confirm League 5 includes player team plus seven AI teams and zeroed stats.
4. Progress to first player fixture and verify match page trigger.
Expected:
- League table renders correctly from season start.
- Player match day reliably routes to `/match`.

Command checks run after this update:
- `npm run build`: passed.
- `npm run lint`: failed due to existing baseline repository lint issues (engine/info modules), with no new EPIC-010-specific lint failures.

Automated still needed (additional):
- Integration test for day-results route branching after AI-only and player-match days.
- Integration test for cup draw reveal flow (`/cup-draw`) and continuation transitions.
- Integration test for day-1 fixture reveal output.

Test status: passed

BUG-023 - League stats page navigation and cup leaderboards on Cups page
Manual steps:
1. Open Career Home and verify `Career Controls > Navigation` includes:
   - `Cups`
   - `League Stats` directly beneath it.
2. Click `League Stats` and confirm navigation to `/career/league-stats`.
3. Verify each domestic league card renders three tables:
   - Top Scorers
   - Top Assists
   - Clean Sheets.
4. Navigate to `/career/cups`.
5. In both League Cup and Champions Cup sections, verify `Player Leaderboards` are shown with:
   - Top Scorers
   - Top Assists
   - Clean Sheets.
6. Advance match days and re-check both pages to confirm leaderboard rows update from simulation data.
Expected:
- New league stats route is accessible from Career Controls.
- League stats page shows all required leaderboard types.
- Cups page now includes cup leaderboard tables for both competitions.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-024 - Updated tactic compatibility matrix values
Manual steps:
1. Open Team Management and note selected defensive/attacking tactic pair.
2. Switch through combinations and verify displayed TC changes align with the new matrix values:
   - Low Block: `20 / 60 / 40 / 80 / 50`
   - Mid Block: `60 / 60 / 70 / 40 / 20`
   - High Press: `80 / 20 / 30 / 70 / 50`
   - Offside Trap: `20 / 60 / 60 / 20 / 90`
   - Zonal: `70 / 50 / 50 / 40 / 40`
3. Save tactics and continue to Career Home.
4. Simulate one or more days and inspect debug output where match profile includes `tc` to confirm updated compatibility values are being applied in simulation flow.
Expected:
- Team Management TC value follows the updated matrix for each tactic pair.
- Career simulation team match profile `tc` values derive from the same updated matrix.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-025 - Clean-sheet recording for 0-conceded matches
Manual steps:
1. Simulate to any day with at least one match ending with a team conceding 0.
2. Open league/cup leaderboard tables and inspect `Clean Sheets`.
3. Confirm the goalkeeper for the 0-conceded team receives +1 clean sheet.
4. Repeat on multiple fixtures and both competition scopes (league and cup).
Expected:
- Every time a team concedes 0, that team’s goalkeeper clean-sheet total increments by 1.
- Clean-sheet rows appear correctly in the relevant competition table.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-026 - Top continue button on day-1 draw controls
Manual steps:
1. Start a fresh career and progress to day-1 draw page (`/cup-draw`).
2. Verify top `Day 1 Draw Controls` row contains:
   - `Generate Cup`
   - `Generate Leagues`
   - `Generate All`
   - `Continue`.
3. Before all draws are revealed, verify top `Continue` is disabled.
4. Reveal all draws, then verify top `Continue` enables.
5. Click top `Continue` and confirm progression matches existing bottom continue behavior.
Expected:
- A top continue control is present beside day-1 generation buttons.
- Top continue shares the same reveal gating and flow behavior as bottom continue.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due to existing repository baseline lint issues in unrelated engine/info modules.

Test status: passed

BUG-030 - League table form circles (5 fixed, oldest to newest)
Manual steps:
1. Open `/career/home`.
2. In League Table, verify a new `Form` column is visible for each team row.
3. Confirm each team row always shows 5 circles.
4. At season start (no played matches), confirm all 5 circles are hollow.
5. Simulate days with league fixtures.
6. Re-check form circles and confirm:
   - leftmost visible result is the oldest of last 5,
   - rightmost visible result is the newest,
   - wins are green, draws are yellow, losses are red.
7. Confirm player-team row highlighting and team-name modal click behavior still work.

Expected:
- League table includes form indicators for all teams.
- Display always contains exactly five circles.
- Form ordering is oldest-to-newest left-to-right.
- Missing history slots remain hollow.
- W/D/L colors match green/yellow/red mapping.
- Existing table interactions remain intact.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Component test for `LeagueTablePanel` form mapping (padding to 5 and left-to-right order).
- Visual regression test for form-circle color states and empty-state rendering.

Test status: passed

BUG-031 - Match Result Type Matrix ranges/weights update
Manual steps:
1. Open `src/game/careerSimulation/constants/simulationConstants.js`.
2. Verify `MRD_RESULT_MATRIX` contains these band IDs in order:
   - `0-4`, `5-8`, `9-16`, `17-26`, `27-40`, `41-55`, `56-72`, `73-99`, `100+`.
3. Verify each row probabilities match requested values.
4. Simulate several match days and check day-results/debug to confirm simulation runs without runtime errors using the updated bands.
Expected:
- Matrix bands/weights exactly match latest request.
- Simulation resolves outcomes normally with updated ranges.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Unit tests for `resolveMrdBand` boundaries at `4`, `8`, `16`, `26`, `40`, `55`, `72`, `99`, `100`.
- Distribution sanity test per band over large simulation samples.

Test status: in progress (manual steps documented, not executed in this pass), failed (lint baseline unchanged)

BUG-032 - Match Result Type Matrix corrected row values
Manual steps:
1. Open `src/game/careerSimulation/constants/simulationConstants.js`.
2. Confirm `MRD_RESULT_MATRIX` row values are:
   - `41-55`: `1 / 14 / 10 / 60 / 15`
   - `56-72`: `0.2 / 5.3 / 7.5 / 69 / 18`
   - `73-99`: `0.1 / 1.8 / 3.6 / 74.5 / 20`
3. Simulate several match days and verify no runtime errors in day results/simulation flow.
Expected:
- Corrected values are present for the three revised ranges.
- Match simulation runs normally using the corrected rows.

Command checks run after this bug fix:
- `npm run build`: passed.
- `npm run lint`: failed due existing repository baseline lint issues in unrelated engine/info modules.

Automated still needed:
- Unit tests for matrix row values and boundary resolution for all MRD bands.
- Distribution sanity test for corrected mid/high MRD rows over large simulation samples.

Test status: in progress (manual steps documented, not executed in this pass), failed (lint baseline unchanged)
