Date: 2026-03-09
Epic: EPIC-011 - Calendar and Career Flow Tidy Up

Feature: FEAT-1101 + FEAT-1111 (Context-aware Continue flow, integrated with day-1 setup gate)
- What was implemented:
  - Added central continue-flow resolver/labels in `src/game/careerFlow/utils/continueFlow.js`.
  - Wired Career Home, Cup Draw, and Day Results to use shared action resolution and shared button labels.
  - Added day-1 gate resolver in `src/game/careerFlow/utils/dayOneSetupGate.js`.
- Engine checklist considered:
  - `gameContext`: used via existing `useGame` state updates and route flow.
  - `button`: reused for all Continue controls.
  - `tooltip`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed, not required for this flow-label/routing work.
- Files changed:
  - `src/game/careerFlow/utils/continueFlow.js`
  - `src/game/careerFlow/utils/dayOneSetupGate.js`
  - `src/game/careerHome/careerHome.jsx`
  - `src/game/cupDraw/cupDraw.jsx`
  - `src/game/careerDayResults/careerDayResults.jsx`
  - `src/game/careerCalendar/components/careerControlPanel.jsx`
- Tradeoffs/follow-up:
  - Continue action resolution is centralized for key career flow pages; any new flow pages should call the same utility.

Feature: FEAT-1102 + FEAT-1103 + FEAT-1105 (Calendar icon/colour event display, tooltip rules, thicker current-day border)
- What was implemented:
  - Replaced text event rows with icon indicators in season calendar day tiles.
  - Added centralized event visual + tooltip rules in `calendarEventDisplay.js`.
  - Implemented cup-match tooltip suppression when player team is not in that cup.
  - Implemented cup-final tooltip override when finalists are known.
  - Increased current-day tile border thickness by 1px.
  - Added global colour tokens for all required event tile/icon mappings.
- Engine checklist considered:
  - `tooltip`: reused engine tooltip component for hover labels.
  - `button`: reused existing month navigation controls.
  - `gameContext`: consumed existing state shape; no new context provider needed.
  - `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: not needed for calendar rendering/hover work.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.jsx`
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `src/game/careerCalendar/utils/calendarEventDisplay.js`
  - `src/assets/scss/ui/colours.scss`
  - `src/game/careerHome/careerHome.jsx`
- Tradeoffs/follow-up:
  - Tooltip accuracy for cup fixtures depends on simulation fixture metadata being present for that day.

Feature: FEAT-1104 + FEAT-1106 + FEAT-1107 + FEAT-1108 (Draw/fixture page tidy-up, top+bottom Continue, remove simulation helper text, fix skill label contrast)
- What was implemented:
  - Updated draw fixture rows to centered paragraph lines in `Team A v Team B` format (no list markup).
  - Ensured Continue controls exist at top and bottom of draw/fixture reveal page.
  - Removed outdated simulation helper text from career control UI.
  - Switched `playerSkillsetBars__label` to dark text for light backgrounds.
- Engine checklist considered:
  - `button`: reused for top/bottom controls and draw actions.
  - `gameContext`: existing state handling retained.
  - `tooltip`, `modal`, `slidePanel`, `toast`, `bars`, `makeDecision`, `rng`, `saveGame`, `timer`: not needed for these presentation updates.
- Files changed:
  - `src/game/cupDraw/cupDraw.jsx`
  - `src/game/cupDraw/cupDraw.scss`
  - `src/game/careerCalendar/components/careerControlPanel.jsx`
  - `src/game/shared/playerSkillsetBars/playerSkillsetBars.scss`
- Tradeoffs/follow-up:
  - Day-1 draw flow keeps draw-generation controls separate from top/bottom Continue controls for clearer progression.

Feature: FEAT-1109 + FEAT-1110 + FEAT-1112 (Start invalid team setup, enforce day-1 setup gate, add debug visibility)
- What was implemented:
  - Career generation now initializes player team-management state as empty/invalid (`savedAt` empty, empty slot assignments, empty tactics).
  - Added validation helpers to determine setup completeness from saved team management.
  - Added enforced routing guard so day 1 cannot progress to day 2 until valid saved setup exists.
  - Added debug panel visibility for setup validity, gate activity, and resolved Continue action.
- Engine checklist considered:
  - `gameContext`: used for persisted state updates and route gating.
  - `button`: reused for navigation actions.
  - `modal`: existing Team Management save-confirm modal remains the save gate.
  - `tooltip`, `slidePanel`, `toast`, `bars`, `makeDecision`, `rng`, `saveGame`, `timer`: not needed for gate logic implementation.
- Files changed:
  - `src/game/careerGeneration/utils/careerGenerator.js`
  - `src/game/teamManagement/utils/teamManagementState.js`
  - `src/game/careerHome/careerHome.jsx`
  - `src/game/cupDraw/cupDraw.jsx`
  - `src/game/careerDayResults/careerDayResults.jsx`
  - `src/game/careerCalendar/components/careerCalendarDebugPanel.jsx`
- Tradeoffs/follow-up:
  - Gate currently keys on day-of-season `1`; if onboarding span expands later, gate utility should accept configurable day ranges.

Feature: FEAT-1113 (Expand and fully fictionalise player/team/place naming pools)
- What was implemented:
  - Expanded player first-name and last-name generation pools to roughly triple prior size.
  - Expanded domestic and foreign team identity pools (prefix/suffix/extra pools) to roughly triple prior size.
  - Replaced prior place-like team prefixes, especially foreign prefixes, with fully fictionalised location-style names.
  - Expanded stadium naming prefixes/suffixes to roughly triple prior size, using fictional/generic world-safe naming.
  - Kept all changes centralised in existing naming pool sources used by procedural generators.
- Engine checklist considered:
  - `rng`: used indirectly via existing random-pick utilities already wired to these pools.
  - `gameContext`, `button`, `bars`, `modal`, `slidePanel`, `toast`, `tooltip`, `makeDecision`, `saveGame`, `timer`: reviewed, not needed for data-pool-only update.
- Files changed:
  - `src/game/playerGeneration/nameGenerationUtils.js`
  - `src/game/careerGeneration/constants/teamIdentityPools.js`
- Tradeoffs/follow-up:
  - This pass focuses on generation pools; optional next pass could add automated checks to flag duplicate names or disallowed real-world place strings.

Bug Fix: BUG-001 (Calendar day tile event colour scope and icon size)
- What was implemented:
  - Updated day-card rendering to apply the primary event visual class to the full day tile.
  - Removed per-indicator coloured pod backgrounds so tile colour carries the event colour.
  - Increased event icon footprint and event-cell spacing for clearer readability.
  - Updated SCSS to use `color.mix` to avoid Sass deprecated global `mix()` warnings.
- Engine checklist considered:
  - `tooltip`: retained existing engine tooltip component for icon hover behavior.
  - `button`, `gameContext`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed, not required for this styling/layout bug fix.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.jsx`
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - For days with multiple event types, tile colour currently follows the first event in the day event list.

Bug Fix: BUG-002 + BUG-003 (Calendar event-icon centering and Team Management skill label contrast)
- What was implemented:
  - Centered calendar event icons within the full lower tile section by changing the event container to centered flex-wrap layout.
  - Adjusted event indicator sizing for consistent icon placement in the centered layout.
  - Added Team Management-scoped override so `playerSkillsetBars__label` uses light text on dark Team Management backgrounds.
- Engine checklist considered:
  - `tooltip`: existing tooltip behavior retained with no functional changes.
  - `button`, `gameContext`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed and not required for these styling bug fixes.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `src/game/teamManagement/teamManagement.scss`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - Global skill-bar labels remain dark by default; Team Management now applies a local light-text override for its dark theme.

Bug Fix: BUG-004 (Calendar day tile icon size to 40x40)
- What was implemented:
  - Increased calendar event indicator slots to `40px x 40px`.
  - Increased calendar icon wrapper size to `40px x 40px`.
  - Increased icon glyph size to maintain readability at the new icon footprint.
- Engine checklist considered:
  - `tooltip`: retained existing tooltip trigger/wiring with no behavior changes.
  - `button`, `gameContext`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed and not required for this size-only UI bug fix.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - Dense event days may visually fill faster due larger icon footprint; centering layout still applies.

Bug Fix: BUG-005 (Tooltip trigger size alignment for 40x40 calendar icons)
- What was implemented:
  - Updated `.seasonCalendar__tooltipTrigger` to explicit `40px x 40px` so tooltip hit-area aligns with the icon wrapper.
  - Added `line-height: 0` on tooltip trigger to avoid inline baseline offsets.
- Engine checklist considered:
  - `tooltip`: reused existing engine tooltip component and only adjusted local trigger sizing.
  - `button`, `gameContext`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed and not required for this CSS-only bug fix.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - Trigger area now intentionally equals icon area; if larger tap targets are desired later, both trigger and indicator can be increased together.

Bug Fix: BUG-006 (Render only player-league playoff icon in calendar)
- What was implemented:
  - Added playoff-event filtering in `SeasonCalendar` so only the playoff tied to the player's league is rendered.
  - Added `playerTeamCompetitionId` prop wiring from Career Home into SeasonCalendar.
  - Updated DayCard event rendering/class selection to use filtered visible events.
- Engine checklist considered:
  - `gameContext`: reused existing career world data flow to supply player competition id.
  - `tooltip`: retained existing tooltip behavior; only event visibility logic changed.
  - `button`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed and not required for this filtering fix.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.jsx`
  - `src/game/careerHome/careerHome.jsx`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - Filtering is currently scoped to playoff events only, as requested.

Bug Fix: BUG-007 + BUG-008 (Calendar tooltip-trigger border removal and 40px width consistency)
- What was implemented:
  - Added season-calendar scoped tooltip trigger overrides to remove inherited shared border/background/shadow styling.
  - Disabled trigger hover transform/shadow in calendar context to prevent visual distortion.
  - Fixed tooltip-wrapped icon sizing consistency by setting fixed trigger flex basis and zero padding (`40px` footprint).
- Engine checklist considered:
  - `tooltip`: reused existing engine tooltip component and resolved issues via local class overrides only.
  - `gameContext`, `button`, `bars`, `modal`, `slidePanel`, `toast`, `makeDecision`, `rng`, `saveGame`, `timer`: reviewed and not required for this CSS-only bug fix.
- Files changed:
  - `src/game/careerCalendar/components/seasonCalendar.scss`
  - `vibe/epics/EPIC-011/bugs.txt`
- Tradeoffs/follow-up:
  - Override is intentionally scoped to calendar tooltip triggers; shared tooltip style remains unchanged for other areas.
